# Changelog

## 0.1.0 - 2026-03-10

- created the initial SCBS monorepo scaffold
- added the `scbs` CLI MVP command surface
- added root config, docs, and CI workflows

## 0.1.1 - 2026-03-11

- aligned `init`, `serve`, `doctor`, and `migrate` with the shipped local durable standalone surface
- updated the checked-in SCBS config and root docs to describe the real local JSON adapter
- restored `SCBS.md` so the root product surface reference resolves again
